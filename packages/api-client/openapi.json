{
    "openapi": "3.0.3",
    "info": {
        "title": "Macros Tracker API",
        "version": "0.1.0",
        "description": "Typed contract consumed by apps/mobile via packages/api-client."
    },
    "paths": {
        "/api/auth/sessions/": {
            "post": {
                "operationId": "createSession",
                "description": "Exchanges a verified Apple identity token for a user and a JWT pair. Creates the account when the Apple subject is unknown, so this one call covers both first-time sign-up and returning login.\n\n**Nonce.** Send the *raw* nonce, not the digest. The client sets `SHA256(nonce)` as lowercase hex on the Apple request and Apple copies that digest into the token; the server hashes the raw value to compare. Sending the digest fails verification.\n\n**Name.** Apple supplies `fullName` on the first authorization only. Send it when present; omitting it never clears a stored name. It is not part of the signed token, so it is stored for display and is not verified.\n\n**Reactivation.** A previously deleted account is restored automatically, with no time limit. Once the purge has removed the row, a sign-in creates a fresh account instead.\n\nRequires no authentication, and is rate limited by IP. Every verification failure returns the same 401 regardless of cause.",
                "summary": "Sign in with Apple",
                "tags": [
                    "auth"
                ],
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": {
                                "$ref": "#/components/schemas/SessionCreateRequest"
                            },
                            "examples": {
                                "FirstSign-in": {
                                    "value": {
                                        "identity_token": "eyJraWQiOiIxRTZWaW9JYU5JIiwiYWxnIjoiUlMyNTYifQ...",
                                        "nonce": "n-0S6_WzA2Mj",
                                        "name": "Alex Hint"
                                    },
                                    "summary": "New user, name supplied",
                                    "description": "Apple sends `fullName` only on the first authorization, so this shape appears exactly once per user."
                                },
                                "ReturningSign-in": {
                                    "value": {
                                        "identity_token": "eyJraWQiOiIxRTZWaW9JYU5JIiwiYWxnIjoiUlMyNTYifQ...",
                                        "nonce": "n-0S6_WzA2Mj"
                                    },
                                    "summary": "Existing user, no name",
                                    "description": "Every later sign-in looks like this. Omitting `name` leaves the stored value untouched."
                                }
                            }
                        },
                        "application/x-www-form-urlencoded": {
                            "schema": {
                                "$ref": "#/components/schemas/SessionCreateRequest"
                            }
                        },
                        "multipart/form-data": {
                            "schema": {
                                "$ref": "#/components/schemas/SessionCreateRequest"
                            }
                        }
                    },
                    "required": true
                },
                "security": [
                    {}
                ],
                "responses": {
                    "201": {
                        "content": {
                            "application/json": {
                                "schema": {
                                    "$ref": "#/components/schemas/Session"
                                },
                                "examples": {
                                    "Session": {
                                        "value": {
                                            "user": {
                                                "id": 1,
                                                "email": "user@privaterelay.appleid.com",
                                                "name": "Alex Hint",
                                                "timezone": "UTC",
                                                "onboarding_completed": false,
                                                "sex": "",
                                                "current_weight_lb": null,
                                                "goal_weight_lb": null,
                                                "goal_timeline_weeks": null,
                                                "training_days_per_week": null,
                                                "dietary_constraints": ""
                                            },
                                            "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
                                            "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
                                            "created": true
                                        },
                                        "summary": "Successful response",
                                        "description": "`created` distinguishes a first run from a returning user. Store both tokens in the Keychain; the refresh token rotates on use."
                                    }
                                }
                            }
                        },
                        "description": ""
                    },
                    "400": {
                        "description": "No response body"
                    },
                    "401": {
                        "description": "No response body"
                    },
                    "429": {
                        "description": "No response body"
                    }
                }
            }
        },
        "/api/auth/sessions/current/": {
            "delete": {
                "operationId": "deleteCurrentSession",
                "description": "Blacklists the refresh token in the body, so it can never mint another access token.\n\n**The refresh token goes in the body of a DELETE**, which is unusual and is the only option there is: blacklisting an access token is not a thing that exists in this scheme. Nothing on the access path consults a blacklist, which is also why signing out does not invalidate the access token the client is holding — that one stays valid until it expires, within 15 minutes. Discard both tokens client-side as well.\n\nAuthenticated, and the token must belong to the caller. Signing out is idempotent from the client's point of view: an already blacklisted or malformed token returns 400 and the client should clear its storage regardless.",
                "summary": "Sign out",
                "tags": [
                    "auth"
                ],
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": {
                                "$ref": "#/components/schemas/SessionDeleteRequest"
                            },
                            "examples": {
                                "SignOut": {
                                    "value": {
                                        "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                                    },
                                    "summary": "Present the stored refresh token"
                                }
                            }
                        },
                        "application/x-www-form-urlencoded": {
                            "schema": {
                                "$ref": "#/components/schemas/SessionDeleteRequest"
                            }
                        },
                        "multipart/form-data": {
                            "schema": {
                                "$ref": "#/components/schemas/SessionDeleteRequest"
                            }
                        }
                    },
                    "required": true
                },
                "security": [
                    {
                        "jwtAuth": []
                    },
                    {
                        "cookieAuth": []
                    }
                ],
                "responses": {
                    "204": {
                        "description": "No response body"
                    },
                    "400": {
                        "description": "No response body"
                    },
                    "401": {
                        "description": "No response body"
                    }
                }
            }
        },
        "/api/auth/sessions/refresh/": {
            "post": {
                "operationId": "refreshSession",
                "description": "Exchanges a valid refresh token for a new access token **and a new refresh token**. Rotation is on, so the presented refresh token is blacklisted by this call: store the returned pair and discard the old one, or the next refresh fails.\n\n**Requires no authentication, by design.** This endpoint exists because the access token has expired, so demanding a valid one here would be a deadlock. The refresh token in the body *is* the credential.\n\nA rejected token returns 401 whether it expired, was already rotated, or was never valid. Reusing a rotated token is the signature of a stolen credential, and the client's only correct response to any 401 here is to sign in again.",
                "summary": "Refresh a session",
                "tags": [
                    "auth"
                ],
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": {
                                "$ref": "#/components/schemas/SessionRefreshRequest"
                            },
                            "examples": {
                                "Refresh": {
                                    "value": {
                                        "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                                    },
                                    "summary": "Present the stored refresh token"
                                }
                            }
                        },
                        "application/x-www-form-urlencoded": {
                            "schema": {
                                "$ref": "#/components/schemas/SessionRefreshRequest"
                            }
                        },
                        "multipart/form-data": {
                            "schema": {
                                "$ref": "#/components/schemas/SessionRefreshRequest"
                            }
                        }
                    },
                    "required": true
                },
                "responses": {
                    "200": {
                        "content": {
                            "application/json": {
                                "schema": {
                                    "$ref": "#/components/schemas/SessionRefresh"
                                },
                                "examples": {
                                    "NewPair": {
                                        "value": {
                                            "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
                                            "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                                        },
                                        "summary": "Successful response",
                                        "description": "`refresh` differs from the one presented. Overwrite both values in the Keychain together."
                                    }
                                }
                            }
                        },
                        "description": ""
                    },
                    "400": {
                        "description": "No response body"
                    },
                    "401": {
                        "description": "No response body"
                    },
                    "429": {
                        "description": "No response body"
                    }
                }
            }
        },
        "/api/health/": {
            "get": {
                "operationId": "health",
                "description": "Reports whether the API can serve traffic. Unlike `/api/ping/`, this executes a real query against the database and returns HTTP 503 if that query fails, so an orchestrator can pull the instance out of rotation. Requires no authentication.\n\nIntended for platform health probes rather than for clients.",
                "summary": "Deployment health check",
                "tags": [
                    "system"
                ],
                "security": [
                    {}
                ],
                "responses": {
                    "200": {
                        "content": {
                            "application/json": {
                                "schema": {
                                    "$ref": "#/components/schemas/Health"
                                },
                                "examples": {
                                    "Healthy": {
                                        "value": {
                                            "status": "ok",
                                            "database": true,
                                            "version": "0.1.0",
                                            "timestamp": "2026-07-23T12:34:56Z"
                                        },
                                        "summary": "Database reachable",
                                        "description": "Every dependency check passed; the instance can serve traffic."
                                    }
                                }
                            }
                        },
                        "description": ""
                    },
                    "503": {
                        "content": {
                            "application/json": {
                                "schema": {
                                    "$ref": "#/components/schemas/Health"
                                },
                                "examples": {
                                    "Unhealthy": {
                                        "value": {
                                            "status": "unhealthy",
                                            "database": false,
                                            "version": "0.1.0",
                                            "timestamp": "2026-07-23T12:34:56Z"
                                        },
                                        "summary": "Database unreachable",
                                        "description": "The process is running but its query failed. Served with 503 so the platform treats the instance as unavailable."
                                    }
                                }
                            }
                        },
                        "description": ""
                    }
                }
            }
        },
        "/api/ping/": {
            "get": {
                "operationId": "ping",
                "description": "Returns a small fixed-shape payload confirming the API is reachable and able to serialize a response. Requires no authentication and touches no database, so it is safe to call from an unauthenticated client and as a container health check.",
                "summary": "API liveness probe",
                "tags": [
                    "system"
                ],
                "security": [
                    {}
                ],
                "responses": {
                    "200": {
                        "content": {
                            "application/json": {
                                "schema": {
                                    "$ref": "#/components/schemas/Ping"
                                },
                                "examples": {
                                    "Healthy": {
                                        "value": {
                                            "status": "ok",
                                            "version": "0.1.0",
                                            "timestamp": "2026-07-23T12:34:56Z"
                                        },
                                        "summary": "Normal response",
                                        "description": "What a reachable API returns."
                                    }
                                }
                            }
                        },
                        "description": ""
                    }
                }
            }
        },
        "/api/uploads/": {
            "post": {
                "operationId": "presignUpload",
                "description": "Returns a short-lived presigned PUT URL and the object key the upload will land at.\n\nThe client uploads straight to object storage rather than through this API, so a slow mobile connection never occupies a server worker. The declared content type and length are signed into the URL: the subsequent PUT must match both exactly or storage rejects it.\n\nStore the returned `key` against the entry. The URL expires; the key does not.",
                "summary": "Authorise a direct photo upload",
                "tags": [
                    "uploads"
                ],
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": {
                                "$ref": "#/components/schemas/PresignUploadRequestRequest"
                            },
                            "examples": {
                                "A2MBJPEG": {
                                    "value": {
                                        "content_type": "image/jpeg",
                                        "content_length": 2097152
                                    },
                                    "summary": "Typical request",
                                    "description": "What the mobile client sends before uploading a camera photo."
                                }
                            }
                        },
                        "application/x-www-form-urlencoded": {
                            "schema": {
                                "$ref": "#/components/schemas/PresignUploadRequestRequest"
                            }
                        },
                        "multipart/form-data": {
                            "schema": {
                                "$ref": "#/components/schemas/PresignUploadRequestRequest"
                            }
                        }
                    },
                    "required": true
                },
                "security": [
                    {
                        "jwtAuth": []
                    },
                    {
                        "cookieAuth": []
                    }
                ],
                "responses": {
                    "200": {
                        "content": {
                            "application/json": {
                                "schema": {
                                    "$ref": "#/components/schemas/PresignUploadResponse"
                                },
                                "examples": {
                                    "SignedAuthorisation": {
                                        "value": {
                                            "url": "https://<account>.r2.cloudflarestorage.com/bucket/entries/1/...",
                                            "key": "entries/1/2f1c9e04-0d5c-4a51-9f0a-3b7c2e6a1d88.jpg",
                                            "expires_in": 300,
                                            "max_bytes": 10485760
                                        },
                                        "summary": "Successful response",
                                        "description": "`url` is a bearer credential valid for `expires_in` seconds. `key` is what gets persisted."
                                    }
                                }
                            }
                        },
                        "description": ""
                    },
                    "400": {
                        "description": "No response body"
                    },
                    "401": {
                        "description": "No response body"
                    }
                }
            }
        },
        "/api/users/me/": {
            "get": {
                "operationId": "getCurrentUser",
                "description": "Returns the user the bearer token resolves to. This is how the client learns who it is signed in as after a cold start, and `onboarding_completed` is what it routes on: false sends the user to onboarding, true to Today.\n\n401 when the token is missing, malformed, or expired — which the client should treat as 'refresh, then retry once'.",
                "summary": "Read the signed-in user",
                "tags": [
                    "users"
                ],
                "security": [
                    {
                        "jwtAuth": []
                    },
                    {
                        "cookieAuth": []
                    }
                ],
                "responses": {
                    "200": {
                        "content": {
                            "application/json": {
                                "schema": {
                                    "$ref": "#/components/schemas/User"
                                },
                                "examples": {
                                    "CurrentUser": {
                                        "value": {
                                            "id": 1,
                                            "email": "user@privaterelay.appleid.com",
                                            "name": "Alex Hint",
                                            "timezone": "Europe/London",
                                            "onboarding_completed": true,
                                            "sex": "male",
                                            "current_weight_lb": "192.00",
                                            "goal_weight_lb": "173.00",
                                            "goal_timeline_weeks": 12,
                                            "training_days_per_week": 4,
                                            "dietary_constraints": ""
                                        },
                                        "summary": "A user partway through settings",
                                        "description": "The four settings fields are null or empty until the user fills them in, which most never will."
                                    }
                                }
                            }
                        },
                        "description": ""
                    },
                    "401": {
                        "description": "No response body"
                    }
                }
            },
            "patch": {
                "operationId": "updateCurrentUser",
                "description": "Partial update of the settings doc 05 moved out of onboarding: goal weight, timeline, training days, and dietary constraints — plus the timezone the client refreshes on launch.\n\n**Partial.** An omitted field is left untouched; an explicit null clears it back to unanswered. That distinction is the reason this is `PATCH` and not `PUT`: a full replace cannot tell the two apart.\n\nOnly these fields are writable. `email` and `name` come from Apple at sign-in, and `onboarding_completed` is set by completing onboarding, so none of them are accepted here — sending them is ignored, not an error. The response is the full user, so the client can replace its cached copy with one round trip.",
                "summary": "Update the signed-in user's settings",
                "tags": [
                    "users"
                ],
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": {
                                "$ref": "#/components/schemas/PatchedUserSettingsRequest"
                            },
                            "examples": {
                                "SetAGoal": {
                                    "value": {
                                        "current_weight_lb": "192.00",
                                        "goal_weight_lb": "173.00",
                                        "goal_timeline_weeks": 12
                                    },
                                    "summary": "Two fields, the rest untouched"
                                },
                                "ClearTheGoal": {
                                    "value": {
                                        "sex": "",
                                        "current_weight_lb": null,
                                        "goal_weight_lb": null,
                                        "goal_timeline_weeks": null
                                    },
                                    "summary": "Explicit nulls clear",
                                    "description": "Omitting these fields would have left the stored values alone."
                                },
                                "RefreshTheTimezone": {
                                    "value": {
                                        "timezone": "Europe/London"
                                    },
                                    "summary": "Sent on app launch"
                                }
                            }
                        },
                        "application/x-www-form-urlencoded": {
                            "schema": {
                                "$ref": "#/components/schemas/PatchedUserSettingsRequest"
                            }
                        },
                        "multipart/form-data": {
                            "schema": {
                                "$ref": "#/components/schemas/PatchedUserSettingsRequest"
                            }
                        }
                    }
                },
                "security": [
                    {
                        "jwtAuth": []
                    },
                    {
                        "cookieAuth": []
                    }
                ],
                "responses": {
                    "200": {
                        "content": {
                            "application/json": {
                                "schema": {
                                    "$ref": "#/components/schemas/User"
                                }
                            }
                        },
                        "description": ""
                    },
                    "400": {
                        "description": "No response body"
                    },
                    "401": {
                        "description": "No response body"
                    }
                }
            },
            "delete": {
                "operationId": "deleteCurrentUser",
                "description": "Deletes the caller's account. Required by both app stores, which will not ship an app that creates accounts without an in-app way to remove them.\n\n**This is reversible.** Signing in again with the same Apple ID restores the account and its data, as long as the account has not yet been purged — there is no separate 'undelete' call. The row is marked deleted rather than destroyed; scheduled destruction is not implemented yet, so nothing is currently erased.\n\nAccess stops at once. The refresh tokens are blacklisted and the account is deactivated, so the access token the client is holding fails on its next use rather than lasting out its 15 minutes. The client should clear its token storage and route to Welcome.\n\nDeleting twice changes nothing: the deletion timestamp keeps its original value. Expect a 401 rather than a second 204, though — the account is deactivated by then, so authentication rejects the retry before this endpoint sees it. Either answer means the same thing to the client, which should clear its tokens and move on.",
                "summary": "Delete the signed-in user's account",
                "tags": [
                    "users"
                ],
                "security": [
                    {
                        "jwtAuth": []
                    },
                    {
                        "cookieAuth": []
                    }
                ],
                "responses": {
                    "204": {
                        "description": "No response body"
                    },
                    "401": {
                        "description": "No response body"
                    }
                }
            }
        }
    },
    "components": {
        "schemas": {
            "BlankEnum": {
                "enum": [
                    ""
                ]
            },
            "Health": {
                "type": "object",
                "description": "Response body for the deployment health check.\n\nDistinct from `PingSerializer` on purpose. Ping answers \"is the process\nup?\"; health answers \"can this process actually serve a request?\", which\nmeans proving the database is reachable. They report different things and a\nshared shape would blur that.",
                "properties": {
                    "status": {
                        "allOf": [
                            {
                                "$ref": "#/components/schemas/HealthStatusEnum"
                            }
                        ],
                        "description": "`ok` when every dependency check passed, `unhealthy` otherwise. An `unhealthy` body is always served with HTTP 503, never 200.\n\n* `ok` - ok\n* `unhealthy` - unhealthy"
                    },
                    "database": {
                        "type": "boolean",
                        "description": "Whether a real query against the default database succeeded. This is an executed round-trip, not a check that settings are present."
                    },
                    "version": {
                        "type": "string",
                        "description": "API version string, mirroring SPECTACULAR_SETTINGS['VERSION']."
                    },
                    "timestamp": {
                        "type": "string",
                        "format": "date-time",
                        "description": "Server time in UTC (ISO 8601) when the check was performed."
                    }
                },
                "required": [
                    "database",
                    "status",
                    "timestamp",
                    "version"
                ]
            },
            "HealthStatusEnum": {
                "enum": [
                    "ok",
                    "unhealthy"
                ],
                "type": "string",
                "description": "* `ok` - ok\n* `unhealthy` - unhealthy"
            },
            "PatchedUserSettingsRequest": {
                "type": "object",
                "description": "What a client may write to its own user.\n\nDeliberately not `UserSerializer` with a couple of fields marked read-only.\nSharing one serializer between the read and the write shape is how\n`onboarding_completed` becomes client-settable: it only takes someone\nrelaxing a `read_only_fields` entry to ship the hole, and nothing about the\nread shape would look wrong in review.\n\nBounds live on the model fields, so `PATCH` and the admin reject the same\nvalues. ModelSerializer copies model validators onto the serializer fields.",
                "properties": {
                    "timezone": {
                        "type": "string",
                        "minLength": 1,
                        "description": "IANA timezone name, e.g. `America/New_York`. The client refreshes this on launch (doc 02), so a user who flies somewhere gets their days bucketed locally. Never a numeric offset — offsets break across DST.",
                        "maxLength": 64
                    },
                    "sex": {
                        "description": "Biological sex, `female` or `male`. Asked during onboarding and stored because editing targets later needs it. Biological rather than gender: it feeds a formula fitted to body composition.\n\n**Clear it with an empty string, not with `null`.** Every other clearable field here takes `null`; this one is a blank-string column and takes `\"\"`. A form that clears itself by sending `null` everywhere gets a 400 on this field alone.\n\n* `female` - Female\n* `male` - Male",
                        "oneOf": [
                            {
                                "$ref": "#/components/schemas/SexEnum"
                            },
                            {
                                "$ref": "#/components/schemas/BlankEnum"
                            }
                        ]
                    },
                    "current_weight_lb": {
                        "type": "string",
                        "format": "decimal",
                        "pattern": "^-?\\d{0,4}(?:\\.\\d{0,2})?$",
                        "nullable": true,
                        "description": "Current body weight in pounds, 85–500. Asked during onboarding and kept, because a target set in Settings weeks later has to be bounded against something. The 85 floor is not a sanity check: below it the suggested calorie range inverts. Null means not answered."
                    },
                    "goal_weight_lb": {
                        "type": "string",
                        "format": "decimal",
                        "pattern": "^-?\\d{0,4}(?:\\.\\d{0,2})?$",
                        "nullable": true,
                        "description": "Target body weight in pounds, 85–500, the same band as the current weight because they measure the same thing. Null clears it back to unanswered."
                    },
                    "goal_timeline_weeks": {
                        "type": "integer",
                        "maximum": 104,
                        "minimum": 1,
                        "nullable": true,
                        "description": "Weeks the user wants to reach the goal weight in, 1–104. A duration rather than a date, so it does not go stale. Null clears."
                    },
                    "training_days_per_week": {
                        "type": "integer",
                        "maximum": 7,
                        "minimum": 0,
                        "nullable": true,
                        "description": "Training days in a typical week, 0–7. Null clears."
                    },
                    "dietary_constraints": {
                        "type": "string",
                        "nullable": true,
                        "description": "Free text, up to 500 characters — 'no dairy, allergic to shellfish'. Read by the advice feature (doc 08), not by target generation. Send an empty string to clear; null is accepted and stored as empty.",
                        "maxLength": 500
                    }
                }
            },
            "Ping": {
                "type": "object",
                "description": "Response body for the API liveness probe.\n\nFields are declared explicitly (never `fields = \"__all__\"`) so the emitted\nOpenAPI schema is precise enough to generate useful client types from.",
                "properties": {
                    "status": {
                        "type": "string",
                        "description": "Always `ok` when the API is reachable."
                    },
                    "version": {
                        "type": "string",
                        "description": "API version string, mirroring SPECTACULAR_SETTINGS['VERSION']."
                    },
                    "timestamp": {
                        "type": "string",
                        "format": "date-time",
                        "description": "Server time in UTC (ISO 8601) when the request was handled."
                    }
                },
                "required": [
                    "status",
                    "timestamp",
                    "version"
                ]
            },
            "PresignUploadRequestRequest": {
                "type": "object",
                "description": "What the client must state before an upload is authorised.\n\nBoth fields are required. Letting either default would mean signing a URL\nwhose constraints the client never committed to, which is the same as not\nconstraining it.",
                "properties": {
                    "content_type": {
                        "type": "string",
                        "minLength": 1,
                        "description": "MIME type of the file about to be uploaded. Must be one of the supported image types; the PUT is rejected if it does not match."
                    },
                    "content_length": {
                        "type": "integer",
                        "minimum": 1,
                        "description": "Exact size in bytes of the file about to be uploaded. Signed into the URL, so the PUT must send precisely this many bytes."
                    }
                },
                "required": [
                    "content_length",
                    "content_type"
                ]
            },
            "PresignUploadResponse": {
                "type": "object",
                "description": "The signed authorisation handed back to the client.",
                "properties": {
                    "url": {
                        "type": "string",
                        "format": "uri",
                        "description": "Presigned S3 PUT URL. Treat as a credential: anyone holding it can upload to this key until it expires."
                    },
                    "key": {
                        "type": "string",
                        "description": "Object key the upload will land at. Store this against the entry — it is the durable identifier, whereas the URL expires."
                    },
                    "expires_in": {
                        "type": "integer",
                        "description": "Seconds until the URL stops being accepted."
                    },
                    "max_bytes": {
                        "type": "integer",
                        "description": "Server-side upload ceiling, echoed so a client can fail fast locally instead of discovering it on a rejected PUT."
                    }
                },
                "required": [
                    "expires_in",
                    "key",
                    "max_bytes",
                    "url"
                ]
            },
            "Session": {
                "type": "object",
                "description": "A created session: who you are, and the tokens to keep being them.",
                "properties": {
                    "user": {
                        "$ref": "#/components/schemas/User"
                    },
                    "access": {
                        "type": "string",
                        "description": "Short-lived bearer token for API calls. Send as `Authorization: Bearer <access>`."
                    },
                    "refresh": {
                        "type": "string",
                        "description": "Long-lived token used to mint a new pair. Rotated on every use and the old one blacklisted, so store it in the Keychain via expo-secure-store and never in AsyncStorage."
                    },
                    "created": {
                        "type": "boolean",
                        "description": "True when this sign-in created the account. The client uses it to distinguish a first run from a returning user."
                    }
                },
                "required": [
                    "access",
                    "created",
                    "refresh",
                    "user"
                ]
            },
            "SessionCreateRequest": {
                "type": "object",
                "description": "What the client presents to sign in.\n\nBoth fields are required. The token alone is not enough: without the nonce\nthere is nothing to prove this token was minted for *this* sign-in attempt\nrather than captured from an earlier one.",
                "properties": {
                    "identity_token": {
                        "type": "string",
                        "minLength": 1,
                        "description": "The `identityToken` from ASAuthorizationAppleIDCredential, as a JWT string. Verified against Apple's published signing keys before anything else happens."
                    },
                    "nonce": {
                        "type": "string",
                        "minLength": 1,
                        "description": "The **raw** nonce generated for this sign-in, not the digest. The client sets `SHA256(nonce)` as lowercase hex on the Apple request; Apple copies that digest into the token verbatim, and the server hashes this value to compare. Sending the digest here fails."
                    },
                    "name": {
                        "type": "string",
                        "description": "Display name, from `fullName` on the credential. Apple supplies it on the first authorization only and never again, so send it when present and omit it otherwise — omitting never clears a stored name. Unverified: Apple puts no name in the identity token, so this is the client's word and is used for display only.",
                        "maxLength": 255
                    }
                },
                "required": [
                    "identity_token",
                    "nonce"
                ]
            },
            "SessionDeleteRequest": {
                "type": "object",
                "description": "What sign-out presents.\n\nThe refresh token, in the body of a DELETE. Unusual, and correct: an access\ntoken cannot be revoked -- there is no blacklist on the access path -- so\nthe refresh token is the only thing there is to destroy.",
                "properties": {
                    "refresh": {
                        "type": "string",
                        "minLength": 1,
                        "description": "The refresh token to blacklist. Must belong to the authenticated user; another user's token is rejected."
                    }
                },
                "required": [
                    "refresh"
                ]
            },
            "SessionRefresh": {
                "type": "object",
                "description": "A refresh token in, a new token pair out.\n\nOne serializer for both directions, because the shapes match. `access` is\nresponse-only, so the request component drops it.",
                "properties": {
                    "refresh": {
                        "type": "string",
                        "description": "The refresh token from the last sign-in or refresh. It is the credential for this call: no `Authorization` header is read, because the whole point of refreshing is that the access token has expired.\n\nThe response contains a **different** refresh token. Store it, and discard the one you sent -- rotation blacklists it immediately."
                    },
                    "access": {
                        "type": "string",
                        "readOnly": true,
                        "description": "A new short-lived access token."
                    }
                },
                "required": [
                    "access",
                    "refresh"
                ]
            },
            "SessionRefreshRequest": {
                "type": "object",
                "description": "A refresh token in, a new token pair out.\n\nOne serializer for both directions, because the shapes match. `access` is\nresponse-only, so the request component drops it.",
                "properties": {
                    "refresh": {
                        "type": "string",
                        "minLength": 1,
                        "description": "The refresh token from the last sign-in or refresh. It is the credential for this call: no `Authorization` header is read, because the whole point of refreshing is that the access token has expired.\n\nThe response contains a **different** refresh token. Store it, and discard the one you sent -- rotation blacklists it immediately."
                    }
                },
                "required": [
                    "refresh"
                ]
            },
            "SexEnum": {
                "enum": [
                    "female",
                    "male"
                ],
                "type": "string",
                "description": "**Clear it with an empty string, not with `null`.** Every other clearable field here takes `null`; this one is a blank-string column and takes `\"\"`. A form that clears itself by sending `null` everywhere gets a 400 on this field alone.\n\n* `female` - Female\n* `male` - Male"
            },
            "User": {
                "type": "object",
                "description": "The signed-in user, as the client is allowed to see them.\n\nOne serializer for the whole read shape, used by `GET /api/users/me/` and\nembedded in the sign-in response. Two serializers describing the same row\nwould generate two client types that drift the first time a field is added\nto one of them.\n\nA ModelSerializer here, and a hand-written Serializer for the request shapes\nbelow. The difference is what the fields are: these mirror model columns and\nthe validators belong on the model, while a refresh token is not a column\nand never will be.\n\nEvery field is read-only. This is the *read* shape; the subset a client may\nwrite is `UserSettingsSerializer`, and keeping them apart is what stops a\nclient from setting `onboarding_completed` on itself.",
                "properties": {
                    "id": {
                        "type": "integer",
                        "readOnly": true
                    },
                    "email": {
                        "type": "string",
                        "format": "email",
                        "readOnly": true,
                        "nullable": true,
                        "description": "May be null: Apple does not guarantee an email, and may supply a private relay address rather than a real one."
                    },
                    "name": {
                        "type": "string",
                        "readOnly": true,
                        "description": "Display name, or empty when Apple never supplied one."
                    },
                    "timezone": {
                        "type": "string",
                        "readOnly": true
                    },
                    "onboarding_completed": {
                        "type": "boolean",
                        "readOnly": true,
                        "description": "Whether the client should route to onboarding or to Today."
                    },
                    "sex": {
                        "readOnly": true,
                        "oneOf": [
                            {
                                "$ref": "#/components/schemas/SexEnum"
                            },
                            {
                                "$ref": "#/components/schemas/BlankEnum"
                            }
                        ]
                    },
                    "current_weight_lb": {
                        "type": "string",
                        "format": "decimal",
                        "pattern": "^-?\\d{0,4}(?:\\.\\d{0,2})?$",
                        "readOnly": true,
                        "nullable": true
                    },
                    "goal_weight_lb": {
                        "type": "string",
                        "format": "decimal",
                        "pattern": "^-?\\d{0,4}(?:\\.\\d{0,2})?$",
                        "readOnly": true,
                        "nullable": true
                    },
                    "goal_timeline_weeks": {
                        "type": "integer",
                        "readOnly": true,
                        "nullable": true
                    },
                    "training_days_per_week": {
                        "type": "integer",
                        "readOnly": true,
                        "nullable": true
                    },
                    "dietary_constraints": {
                        "type": "string",
                        "readOnly": true
                    }
                },
                "required": [
                    "current_weight_lb",
                    "dietary_constraints",
                    "email",
                    "goal_timeline_weeks",
                    "goal_weight_lb",
                    "id",
                    "name",
                    "onboarding_completed",
                    "sex",
                    "timezone",
                    "training_days_per_week"
                ]
            }
        },
        "securitySchemes": {
            "cookieAuth": {
                "type": "apiKey",
                "in": "cookie",
                "name": "sessionid"
            },
            "jwtAuth": {
                "type": "http",
                "scheme": "bearer",
                "bearerFormat": "JWT"
            }
        }
    }
}