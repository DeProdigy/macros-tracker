# Approved MVP exports

This directory holds visual artifacts that Alex has approved for implementation.

Use the naming rule from the canonical visual-design document:

`<flow>-<screen>-<state>-v<revision>.png`

Add each export to [`../../README.md`](../../README.md), including its source, status, approval, and any known limitations. Candidate files do not belong here.
