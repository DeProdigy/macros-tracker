# Editable design sources

Place original editable design files here. Keep their original filenames.

Record the source file and design session in [`../README.md`](../README.md). Do not treat a source file as approved merely because it exists here. Approved implementation references are exported into `../exports/mvp/`.
